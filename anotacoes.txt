O que está dentro da pasta app é roteado

/ = Pasta app (raíz)
@ = Pasta src


Para criar uma página not found, loading, error, essas, deve criar uma pasta em src chamada components




export default function Loading(){
    return(
        <>
            div.container-notfound>div.flex-notfound>h2{Carregando}
        </>
    )
}